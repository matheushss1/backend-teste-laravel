# Landing Page React/NextJS recebendo informações do backend Laravel

> Rodar o Backend
```bash
cd backend && composer install && php artisan migrate && php artisan serve
```

> Rodar o Frontend
```bash
cd frontend && npm install && npm run dev
```
